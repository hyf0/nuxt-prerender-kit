---
title: Installation
description: How to install and configure Nuxt Prerender Kit.
seo:
  title: Installation
  description: Install and configure Nuxt Prerender Kit in your Nuxt project.
---

## 1. Install the package

::code-group
```bash [pnpm]
pnpm add nuxt-prerender-kit
```

```bash [npm]
npm install nuxt-prerender-kit
```

```bash [yarn]
yarn add nuxt-prerender-kit
```

```bash [bun]
bun add nuxt-prerender-kit
```
::

## 2. Add to Nuxt config

Add `nuxt-prerender-kit` to the `modules` section of your `nuxt.config.ts`:

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  modules: ['nuxt-prerender-kit'],
})
```

That's it! `usePrerenderData` is auto-imported and ready to use.
